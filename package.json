{
  "name": "lotus-tree",
  "version": "2.0.0",
  "dependencies": {
    "discord.js": "^13.0.0",
    "mariadb": "^2.5.4",
    "mysql2": "^2.3.0",
    "pg": "^8.7.1",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.6.5",
    "sqlite3": "^5.0.2",
    "tedious": "^11.4.0"
  },
  "scripts": {
    "start": "node lib",
    "dev": "nodemon",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel --root-mode upward src --out-dir lib"
  },
  "pre-commit": ["build"],
  "devDependencies": {
    "nodemon": "^2.0.15",
    "pre-commit": "^1.2.2"
  }
}
